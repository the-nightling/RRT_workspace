% Matlab script to build graph from
% adjacency matrix and find shortest path
% between two nodes

% copy data from GPU to local PC
pathToScript = fullfile(pwd,'copyData.sh');
system([pathToScript]);
